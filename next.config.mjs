/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/spirit-niche',
  images: { unoptimized: true },
  reactStrictMode: true,
};

export default nextConfig;
